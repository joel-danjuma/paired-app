"use client";
import React from "react";

const Map = () => {
  const [value, setValue] = React.useState(" ");
  return (
    <section>
      <form>{/* <SearchBox accessToken={<your access token here>} /> */}</form>
    </section>
  );
};

export default Map;
